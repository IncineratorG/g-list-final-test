export class FirebaseTransformer {
  static toShoppingLists(sharedShoppingLists) {
    const shoppingLists = [];
    sharedShoppingLists.forEach((sharedListData, sharedListId) => {
      const {shoppingList} = sharedListData;
    });
  }
}
