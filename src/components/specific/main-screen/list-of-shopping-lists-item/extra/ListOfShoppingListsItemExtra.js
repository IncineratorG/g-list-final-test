import {View} from 'react-native';
import React from 'react';

export const ListOfShoppingListsItemExtra = ({
  styles,
  listItem,
  onItemPress,
}) => {
  return <View style={styles.mainContainer} />;
};
